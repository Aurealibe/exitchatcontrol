/* Single source of truth for canonical URLs — consumed by vite.config.ts
   (sitemap) and react-ssg.config.ts (prerender paths). */
export const ORIGIN = 'https://exitchatcontrol.org'
export const PATHS = ['/']
